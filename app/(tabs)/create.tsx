import { View, Text } from 'react-native';

export default function CreateJobScreen() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Create Job Screen (Coming Soon)</Text>
    </View>
  );
}